# Stellar Codex does not read cloud API keys from the environment in the client build.
# Add keys in the app: Settings → AI (encrypted vault, BYOK).
#
# Optional: only if you add a server-side proxy later, those secrets live on the host, not here.
